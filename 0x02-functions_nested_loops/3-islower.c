#include "main.h"

/**
 * _islower -> checks if characte is lowercase
 * @c: a character argument
 * Return: 1 and 0 depending on condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
